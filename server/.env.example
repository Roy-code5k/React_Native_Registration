PORT=5000
NODE_ENV=development

# MongoDB Connection String
# 1. Production Mode: Remote MongoDB Atlas URI
# Example:
MONGO_URI=mongodb+srv://royhrituraj325_db_user:<db_password>@cluster0.b83whfl.mongodb.net/feedants_competition?retryWrites=true&w=majority&appName=Cluster0

# 2. Zero-Config Evaluation Mode:
# Leave MONGO_URI blank or comment it out to automatically use an embedded in-memory MongoDB instance
# (100% plug-and-play without prerequisite database installation).
# MONGO_URI=

# JWT Secrets
JWT_SECRET=feedants-super-secret-key-2026
JWT_EXPIRES_IN=7d
